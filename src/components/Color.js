import React, { useEffect } from 'react';

const Color = () => {

    useEffect(() => {
        const callback = e => {
        
            if()
        }
    } [] )

    window.addEventListener("mousemove", callback)

    return (
        <div className='color'>
            Color
        </div>
    );
};

export default Color;